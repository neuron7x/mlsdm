# MLSDM Core Runtime Dependencies
# These are the ONLY dependencies required to run MLSDM in production
# Sync with pyproject.toml [project.dependencies]

# Core libraries
numpy>=2.0.0
sentence-transformers>=3.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
pyyaml>=6.0.1
pydantic>=2.0.0
prometheus-client>=0.20.0
tenacity>=8.2.3
requests>=2.32.3
psutil>=5.9.0

# Security: Pin minimum versions for indirect dependencies with known vulnerabilities
certifi>=2024.7.4
cryptography>=43.0.1
jinja2>=3.1.6
urllib3>=2.2.2
setuptools>=78.1.1
idna>=3.7

# NOTE: Development dependencies (pytest, mypy, ruff, etc.) are in requirements-dev.txt
# NOTE: OpenTelemetry is optional - install with: pip install -e ".[tracing]"
# NOTE: Visualization tools are optional - install with: pip install -e ".[visualization]"
