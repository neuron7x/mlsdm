# =============================================================================
# MLSDM Comprehensive Development Requirements
# =============================================================================
#
# This file provides a complete development environment with all dependencies.
#
# RECOMMENDED INSTALLATION:
#   For minimal library install:     pip install -e .
#   For library + observability:     pip install -e ".[observability]"
#   For development environment:     pip install -e ".[all-dev,observability]"
#   For this complete setup:         pip install -r requirements.txt
#
# NOTE: This file includes core + observability + dev/test tools
#       For production deployments, use targeted extras from pyproject.toml
#
# =============================================================================

# Core runtime dependencies - defined in pyproject.toml [project.dependencies]
# Install with: pip install -e .
-e .

# Observability (distributed tracing) - optional but recommended for production
opentelemetry-api>=1.24.0,<2.0.0
opentelemetry-sdk>=1.24.0,<2.0.0

# Development and testing tools
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=4.1.0,<5.0.0
hypothesis>=6.98.3,<7.0.0
httpx>=0.27.0,<1.0.0
ruff>=0.4.10,<1.0.0
mypy>=1.10.0,<2.0.0
types-PyYAML>=6.0.0,<7.0.0
types-requests>=2.31.0,<3.0.0
locust>=2.29.1,<3.0.0

# Security auditing tools
pip-audit>=2.6.0,<3.0.0
deptry>=0.12.0,<1.0.0

# =============================================================================
# Security Constraints for Transitive Dependencies
# =============================================================================
# These pins ensure minimum secure versions for indirect dependencies
# that have known vulnerabilities in older versions
#
certifi>=2024.7.4
cryptography>=43.0.1
jinja2>=3.1.6
urllib3>=2.2.2
setuptools>=78.1.1
idna>=3.7
